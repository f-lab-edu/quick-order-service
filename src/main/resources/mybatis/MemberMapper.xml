<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.quickorderservice.mapper.MemberMapper">

    <select id="selectAllMembers" resultType="com.quickorderservice.dto.member.MemberDTO">
        SELECT id, password, name, tel, email, address
        FROM members
    </select>

    <select id="selectMemberById" resultType="com.quickorderservice.dto.member.MemberDTO">
        select id, password, name, tel, email, address
        from members
        where id = #{id}
    </select>

    <insert id="insertMember">
        insert into members (id,password,name,tel,email,address)
        values (#{id},#{password},#{name},#{tel},#{email},#{address})
    </insert>

    <delete id="deleteMember">
        delete from members where id = #{id}
    </delete>

    <update id="updateMember">
        update members set
        name = #{name},
        tel = #{tel},
        email = #{email},
        address = #{address},
        updatedAt = now()
        where id = #{id}
    </update>

    <update id="updateMemberPassword">
        update members set
        password = #{password},
        updatedAt = now()
        where id = #{id}
    </update>

</mapper>